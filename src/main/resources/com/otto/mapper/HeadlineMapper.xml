<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.otto.mapper.HeadlineMapper">

    <resultMap id="BaseResultMap" type="com.otto.pojo.Headline">
            <id property="hid" column="hid" />
            <result property="title" column="title" />
            <result property="article" column="article" />
            <result property="type" column="type" />
            <result property="publisher" column="publisher" />
            <result property="pageViews" column="page_views" />
            <result property="createTime" column="create_time" />
            <result property="updateTime" column="update_time" />
            <result property="version" column="version" />
            <result property="isDeleted" column="is_deleted" />
    </resultMap>

    <sql id="Base_Column_List">
        hid,title,article,type,publisher,page_views,
        create_time,update_time,version,is_deleted
    </sql>

    <select id="selectMyPage" resultType="java.util.Map">
        select
            hid, title, type, page_views pageViews, timestampdiff(HOUR, create_time, NOW()) pastHours, publisher
        from news_headline
        <where>
            is_deleted = 0
            <if test="portalNewsVo.keyWords != null and portalNewsVo.keyWords!= ''">
                and title like concat('%', #{portalNewsVo.keyWords}, '%')
            </if>
            <if test="portalNewsVo.type != null and portalNewsVo.type != 0">
                and type = #{portalNewsVo.type}
            </if>
        </where>
    </select>

    <select id="selectHeadlineDetail" resultType="java.util.Map">
        select
            hid, title, article, type, t.tname typeName, page_views pageViews,
            TIMESTAMPDIFF(HOUR, create_time, NOW()) pastHours, publisher, u.nick_name author,
            h.version
        from news_headline h
                 left join news_type t on h.type = t.tid
                 left join news_user u on h.publisher = u.uid
        where h.hid = #{hid} and h.is_deleted = 0
    </select>
</mapper>
